# Development Workflow Plugin

A LLM plugin for development workflow.

## Usage

Use this folder directly as the plugin source directory.  

For Cursor, clone this repo to `~/.cursor/plugins/local`